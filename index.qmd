---
pagetitle: "Book club"
format: 
  html:
    embed-resources: true
editor_options: 
  chunk_output_type: console
---

```{r}
#| label: load-packages
#| include: false

library(readr) ; library(dplyr) ; library(gt) ; library(gtExtras)
```

```{r}
read_csv("data.csv") |> 
  arrange(desc(Reviewed)) |> 
  gt() |> 
  tab_header(
    title = md("***Book club reads***")
  ) |>
  opt_align_table_header(align = "left") |> 
  opt_interactive(
    use_compact_mode = TRUE
  ) |> 
  gt_theme_dot_matrix() 
```